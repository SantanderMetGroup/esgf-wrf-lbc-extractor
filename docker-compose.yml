version: '3'
services:
  wps:
    build: .
    image: zequihg50/esgf_wrf_lbc_extractor
    ports:
      - "5000:5000"


# docker-compose build
# docker-compose up
# docker-compose down
# docker-compose rm
